# Handover — v1.6.0 released: gpt-5.6-sol is the default model, live-verified end-to-end

## TL;DR
Reviewed, committed, and **released `codex-claude-bridge@1.6.0`** — the GPT-5.6 Sol work implemented (uncommitted) in the previous session (`2026-07-09-01-gpt-5-6-sol-default-uncommitted.md`). Default and `"latest"` now resolve to `gpt-5.6-sol`; `gpt-5.5` is the documented fallback; Codex SDK/CLI bundle is 0.144.0. npm `latest` = 1.6.0 verified, tag `v1.6.0` pushed, GH release live, npx cache warmed with a real Sol-served review. Chose **1.6.0 over 1.5.3** because a default-model change is user-visible behavior, not a patch.

## What this session did (review → ship)
1. **Line-by-line review of the inherited uncommitted diff** (14 tracked files). Verdict: clean. Notable: `CODEX_DEFAULT_MODEL` now derives from `RECOMMENDED_MODELS.codex[0]` and the ISS-009 fallback tip picks the first recommended model that differs case-insensitively from the failed one — future flagship rollovers are a one-constant edit. Remaining `gpt-5.4` refs are all explicit-pin test fixtures (correct per L-006 recommend-not-enforce). Independently re-ran the full gate: 809/809 tests, lint, typecheck, build all green.
2. **Independent live probe (pre-commit)**: planted-bug diff (SQL injection + inverted expiry) through the fresh-dist CLI with NO model override → stderr `resolved model: gpt-5.6-sol (requested: default)`, verdict reject, both bugs caught with correct lines/fixes, `provider: codex`, `review_mode: failover`.
3. **"How do we know it's Sol?" evidence chain** (worth keeping): (a) Codex's own rollout file `~/.codex/sessions/2026/07/09/rollout-…-<session_id>.jsonl` records `"model":"gpt-5.6-sol"` in the `turn_context` event plus `cli_version: 0.144.0` — Codex's record, not the bridge's echo; (b) Codex fail-hard behavior rules out silent substitution (0.142.5 rejected Sol with "requires a newer version of Codex" earlier today; 0.104.0 historically rejected gpt-5.5) — unlike agy, which silently falls back; (c) epistemic floor: server-side slug routing is unverifiable for any hosted provider.
4. **Bridge precommit gate on the staged diff**: `ready_to_commit: true`, 0 blockers/warnings, 2 chunks, served by Sol.
5. **Shipped**: commit `d8e180e` (feat: default reviews to gpt-5.6-sol…), `18fd5a9` (bump 1.6.0), tag `v1.6.0`, pushed. `npm publish` (prepublishOnly re-ran build+typecheck+lint+809 tests). GH release: https://github.com/AmirShayegh/codex-claude-bridge/releases/tag/v1.6.0
6. **Post-publish verification**: `npx -y codex-claude-bridge@latest` review of the probe diff → Sol resolved from default, reject, 2 findings. npx cache is warm — next session's MCP server runs 1.6.0.

## Deliberate exclusions from the commit
- **`AGENTS.md` (untracked, pre-existing)** — it is a corrupted blind find-and-replace of CLAUDE.md ("codex-Codex-bridge", "Never mention \"Codex\", \"Codex\"", nonsense workflow diagram). Do NOT commit as-is; either rewrite properly for a Codex audience (coder = Codex, reviewer = the bridge's Codex backend, package name stays `codex-claude-bridge`) or delete.
- **`.codex/config.toml` (untracked, pre-existing)** — registers the published bridge as an MCP server for Codex sessions; content is correct (`npx -y codex-claude-bridge@latest`). Left untracked pending a decision on whether to version it.

## State
- `main` = this handover's commit (after `18fd5a9`), clean apart from the two untracked files above; tag `v1.6.0`; npm `latest` = 1.6.0; GH release live.
- 809 tests, gate green (run three times this session: manual, prepublishOnly, plus the prior session's runs).
- MCP servers in any currently-open client session still run pre-Sol code until respawned; the CLI and fresh npx path run 1.6.0.
- Local `~/.local/bin/codex` = 0.144.0 (updated last session). Codex app still needs a restart to pick up its CLI update + the newly registered OpenAI docs MCP (outside this repo's scope).
- Open issues unchanged: ISS-018, ISS-020, ISS-022, ISS-024 — all have PHASE 12 tickets (T-033..T-037).

## What's next (unchanged from triage handover)
1. **T-034** — config-staleness warning (~20 lines, unblocks T-035).
2. **T-033** — plan-finding agreement matching (start with live two-provider plan data, T-029 method).
3. Then T-035 → T-036 → T-037 per `2026-07-02-04-triage-phase12.md`.
`/story auto T-034 T-033` remains a sensible next autonomous run.
