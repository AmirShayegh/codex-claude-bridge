# Handover: v1.8.0 released

Follows `2026-09-15-01-peer-reports-fixed-probe-loop.md`.

- PR #12 merged to `main` with a merge commit (0d9d868); tag `v1.8.0` pushed; GitHub release published; `codex-claude-bridge@1.8.0` published to npm with tag `latest`.
- Release notes carry the `require_cwd` default-true behavior change and the Gemini fresh-failure limitation.

## Next
1. Ask the three peer sessions (care2talk-ba, ledger-integrity-protocol, cpm-f2) to `npx codex-claude-bridge@latest` and retest; their reports were the source of ISS-042..049.
2. Watch for `require_cwd` complaints from single-repo users; the opt-out is `"require_cwd": false`.
3. Deferred: cross-instance history lookup (ISS-035/ISS-018); Gemini partial-session id on fresh failures.
