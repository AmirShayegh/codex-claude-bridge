# Handover — Backlog triaged into PHASE 12; tickets written implementation-grade for weaker models

## TL;DR
Post-v1.5.2 triage session (continues `2026-07-02-03-v152-release.md`, which covers the release itself). All 4 open issues triaged, a new **PHASE 12 "Review Quality & Config Backlog"** (`backlog2`, inserted after Hardening) created with **T-033..T-037**, and every ticket then rewritten to implementation-grade detail on the explicit expectation that **less capable models will implement them**. No code changed this session-segment; `main` = `a44a309`, clean, pushed. npm `latest` = 1.5.2.

## Triage outcomes
- **ISS-024** (plan deliberation can never agree) — kept medium → **T-033**. Highest quality-per-effort: half of deliberation's surface (plans) silently returns agreed:[] always.
- **ISS-022** (config staleness) — **bumped low → medium** (bit two dogfood sessions, sits in the install/troubleshoot path) and **split**: T-034 (cheap one-time staleness warning, ~20 lines) + T-035 (full per-call reload, blockedBy T-034). The six requirements from the 2026-07-02 deliberate-deep review of a naive reload plan are baked into T-035 as acceptance criteria.
- **ISS-018** (cwd-dependent reviews.db) — kept low → **T-036**, with note N-003's reviewed findings folded in (CRITICAL: WAL means migration must be `VACUUM INTO`, never file copy; project-keyed subdir decision made — do not re-litigate).
- **ISS-020** (cross-chunk blindness) — kept low → **T-037**, framed as an EXPERIMENT ticket: build a measurement harness first (`git diff cdd1776~7..cdd1776` reproduces the 10/10-false-positive reference diff), then evaluate manifest/epistemic-prompt/downgrade-pass variants with before/after FP counts.

## Ticket-writing conventions applied (keep doing this for weaker implementers)
Each ticket now contains: exact file/function/line anchors; the bug MECHANISM not just the symptom; pre-made decisions marked "implement, don't re-litigate"; ordered steps with "do this FIRST" guards on data-gathering; inlined trap warnings (L-014 required-schema rule + live-Codex-probe requirement wherever a model-facing schema might change; WAL data-loss regression test mandated in T-036; config-layer dependency direction; stderr-only logging); concrete acceptance criteria including the live repro each fix must make pass; and the repo conventions (strict TS, Result<T>, ESM .js suffixes, TDD, no AI names in commits) repeated verbatim since a fresh session may lack them.

## Recommended execution order
1. **T-034** — quick win (~20 lines + tests), unblocks T-035.
2. **T-033** — flagship quality fix; MUST start with live two-provider plan data (3+ runs, save raw findings as a note, T-029 method).
3. **T-035** — reload, requirements already reviewed.
4. **T-036** — db relocation; read N-003 first; the WAL regression test is non-negotiable.
5. **T-037** — experiment; needs Codex/Gemini usage headroom for repeated harness runs.
`/story auto T-034 T-033` is a sensible first autonomous run.

## State
- `main` = `a44a309`, clean, pushed. npm `latest` = 1.5.2, npx cache warm (next session's MCP server runs 1.5.2).
- Open issues: ISS-018, ISS-020, ISS-022 (now medium), ISS-024 — every one has a linked PHASE 12 ticket via relatedTickets.
- 809 tests, gate green as of the v1.5.2 release earlier today.
- Commits this segment: `7236be4` (triage + phase + tickets), `a44a309` (ticket detail expansion).

## Context worth keeping
- T-034/T-035 split rationale: the warning alone kills the UX trap with zero rebuild risk; reload carries the composite-lifecycle/security surface and got a two-provider revise — hence the blockedBy ordering.
- T-037's ground truth: all 10 findings from the 2026-07-01 Hardening-diff review were verified false positives (list in ISS-020); LLM variance means 2 runs per variant minimum (agreed/divergent counts swung 5/1 → 2/7 on identical input earlier today).
- Deliberation self-review of the v1.5.2 diff was approve/approve with 0 findings — the bridge reviewing its own patches continues to work well as a pre-commit gate.