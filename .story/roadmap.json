{
  "blockers": [],
  "date": "2026-04-23",
  "phases": [
    {
      "description": "MCP server over stdio, five tool handlers (review_plan, review_code, review_precommit, review_status, review_history), Codex SDK wrapper with thread/session management, Zod config loader, SQLite storage for reviews and sessions, git diff utilities, and chunking module. Shipped v0.1.0 (tagged d67f742). Documented across handovers 2026-02-18 project-scaffold through 2026-02-19 phase4-publish-prep.",
      "id": "foundation",
      "label": "PHASE 1",
      "name": "Foundation",
      "summary": "Core MCP server + 5 tools + SDK + SQLite (v0.1.0)"
    },
    {
      "description": "Rewrite prompt builders with severity rubrics (critical/major/minor/suggestion/nitpick), config injection (project_context, review_standards), output discipline (JSON-only, no markdown), and checklists per review type. Map Codex SDK errors to actionable messages (AUTH_ERROR, MODEL_ERROR, RATE_LIMITED, NETWORK_ERROR, CODEX_TIMEOUT). Shipped v0.2.0. Documented in handover 2026-02-20-prompt-quality-and-error-messages.",
      "id": "quality",
      "label": "PHASE 2",
      "name": "Quality",
      "summary": "Prompt rewrite + severity rubrics + actionable errors (v0.2.0)"
    },
    {
      "description": "README overhaul leading with the $0 ChatGPT-subscription auth path. Standalone CLI via Commander (review-plan, review-code, review-precommit subcommands) sharing prompt builders, Codex client, and config loader with the MCP server. Free-tier SDK spike proved no code adapter needed (SDK reads OAuth from ~/.codex/auth.json). Shipped v0.1.2. Documented in handovers 2026-02-20-readme-and-doc-cleanup and 2026-02-20-standalone-cli.",
      "id": "adoption",
      "label": "PHASE 3",
      "name": "Adoption",
      "summary": "README $0 pitch + standalone Commander CLI + free-tier docs (v0.1.2)"
    },
    {
      "description": "Wire chunking module into review pipeline for large diffs (ce058b3). Add auto_diff parameter to review_code to match review_precommit ergonomics (37987dc). Validate diff input, provide actionable timeouts, auto-discover GitHub Copilot instruction files as project context (55e8692). Two known defects remain: multi-chunk session orphaning on partial failure, and non-atomic saveReview + markSessionCompleted. Shipped piecewise across v0.1.2 and v0.2.0.",
      "id": "reliability",
      "label": "PHASE 4",
      "name": "Reliability",
      "summary": "Chunking wired + auto_diff + validation + defects to fix"
    },
    {
      "description": "Keep default model current with OpenAI releases. Make install instructions cache-safe so npx re-resolves on every launch. v0.3.0 shipped gpt-5.5 as the default (commit 5ea2f56), pinned @latest in all README install/usage examples, updated the model table to include gpt-5.5 (400K context in Codex) while keeping gpt-5.4 as a documented fallback, and normalized the bin path + gitignored the temp/ working folder (commit 98eedf5). Recurring phase: bumps on future OpenAI releases land here.",
      "id": "model-currency",
      "label": "PHASE 5",
      "name": "Model Currency",
      "summary": "Default model + install pin currency (v0.3.0 = gpt-5.5)"
    },
    {
      "description": "Make the bridge provider-pluggable: route reviews to Google Gemini (via the Antigravity `agy` CLI on a Google AI Pro subscription) as a first-class alternative to OpenAI Codex — with the MCP tool contract, structured-output schemas, session-continuity model, and zero-config install all unchanged. Path A (wrap `agy --print`) was chosen over the raw `@google/genai` API for $0-on-existing-subscription + native resumable sessions. Work = a path-independent ReviewBackend refactor (~75% of effort) + the agy-backed Gemini backend. Designed to not preclude dual/cross-review (run Codex + Gemini, merge with per-finding provenance) as a later step.",
      "id": "multi-provider",
      "label": "Multi-Provider",
      "name": "Multi-Provider",
      "summary": "Pluggable review backends; Gemini via agy CLI alongside Codex"
    },
    {
      "description": "Ship the multi-provider work to users as v1.1.0. The Gemini backend (T-013→T-020) and the C1/M1/m1 + ISS-006/007 hardening are committed and pushed to main, but npm `latest` is still 1.0.0 (Codex-only) and the README/package.json are written as Codex-only. This phase updates the user-facing docs/metadata for multi-provider and cuts + publishes the release.",
      "id": "release-1-1",
      "label": "RELEASE",
      "name": "Multi-Provider Release",
      "summary": "Docs + metadata for multi-provider, then bump/tag/publish v1.1.0"
    },
    {
      "description": "Automatic fallback between review providers when one is unavailable. Surfaced live: Codex hit its usage limit (gpt-5.5) and rejected gpt-5.4 on the ChatGPT tier, while the Gemini backend was fully working — exactly the case where the bridge should transparently fail over instead of returning an error. Includes the error-classification accuracy fixes that failover triggering depends on, plus the failover backend itself (provider provenance + session handling).",
      "id": "failover",
      "label": "FAILOVER",
      "name": "Provider Failover",
      "summary": "Auto-fallback to the other provider when one is out of usage/unavailable"
    },
    {
      "description": "Multi-provider deliberation for high-stakes reviews (idea borrowed from LLMtium). Instead of one provider reviewing (with failover), both providers review the same plan/diff independently and the bridge surfaces a structured agreement/disagreement map — findings both flag are high-confidence, findings only one flags need judgment. Claude Code (the caller) synthesizes. Opt-in via config mode; scoped to review_plan + review_code.",
      "id": "deliberation",
      "label": "DELIBERATION",
      "name": "Deliberation",
      "summary": "Both providers review in parallel; bridge surfaces agree/diverge; Claude synthesizes"
    },
    {
      "description": "Drain the open issue backlog in dependency order: fix diff validation (ISS-005), fix deliberation session semantics at the backend seam (ISS-011/017), make deliberation visible and honest in results (ISS-010/016/012-marker), redesign agreement matching with live two-provider data (ISS-013), deliberate-deep polish (ISS-012-chunking/014/015), config no-op audit (ISS-004), MODEL_ERROR tip refinement (ISS-003). Sequenced so shared-root-cause issues fix together, seam changes land before features built on them, and data-dependent design waits for live probe data.",
      "id": "hardening",
      "label": "PHASE 11",
      "name": "Hardening",
      "summary": "Issue-backlog drain: diff validation, deliberation session semantics + visibility, agreement quality, config audit"
    },
    {
      "description": "Make the review loop useful for teams reviewing GitHub PRs. Adds a sixth MCP tool (review_pr) and a fourth CLI subcommand (review-pr). Fetches PR diff and metadata via `gh pr diff` and `gh pr view --json`, feeds PR title + description + diff to a PR-specific prompt, and returns scope-match and description-match as regular findings (not top-level fields, to keep the result type consistent with review_code). No storage changes needed.",
      "id": "team-integration",
      "label": "PHASE 6",
      "name": "Team Integration",
      "summary": "review_pr tool + CLI, PR diff + metadata via gh"
    },
    {
      "description": "Named review presets (security-audit, quick-scan, performance, pre-commit, api-review) as sugar over focus + depth + checklist parameters. Custom presets definable in .reviewbridge.json. MCP progress notifications for long-running reviews, gated on a research spike that confirms Claude Code actually renders them — if not, that work is cut.",
      "id": "polish",
      "label": "PHASE 7",
      "name": "Polish",
      "summary": "Review presets + MCP progress notifications (gated)"
    }
  ],
  "title": "codex-claude-bridge"
}
